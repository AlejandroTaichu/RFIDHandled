<?xml version="1.0" encoding="utf-8"?>
    <manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.hardware.usb.host" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SCANNER" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElTerminali"
        tools:targetApi="31">


        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.adminoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.inventory.InventoryActivity"
            android:parentActivityName=".ui.warehouse.WarehouseSelectionActivity">
        </activity>

        <activity
            android:name=".ui.count.CountActivity"
            android:exported="false" />

        <activity
            android:name=".ui.product.ProductActivity"
            android:exported="false" />

        <activity
            android:name=".ui.warehouse.WarehouseSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.match.activity.ProductMatchActivity"
            android:exported="false" />

        <activity
            android:name=".ui.match.activity.MatchOptionsActivity"
            android:exported="false" />

        <activity android:name=".ui.count.activity.RFIDSettingsActivity"
            android:exported="false"/>


    </application>

</manifest>